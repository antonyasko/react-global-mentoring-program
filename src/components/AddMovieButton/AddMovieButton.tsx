import React from 'react';

import './AddMovieButton.scss';

function AddMovieButton(): JSX.Element {
  return (
    <button type="button" className="add-movie">
      + add movie
    </button>
  );
}

export default AddMovieButton;
